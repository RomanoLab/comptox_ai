import networkx as nx

from .graph import Graph


class Subgraph(Graph):
    """
    A graph that consists of a subset of ComptoxAI's full graph database.
    """
    def __init__(self, nodes, edges, nx):
        pass
